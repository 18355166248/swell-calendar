# Claude / Codex Entry

> 开始任何实现、重构、评审或文档修改前，先读根目录 [AGENTS.md](./AGENTS.md)。

## 规则

- 本文件不再维护独立规则，避免与 `AGENTS.md` 分叉
- 若 `AGENTS.md` 与其他文档冲突，先按 `AGENTS.md` 的导航顺序继续定位一级真源
- 只有当 `AGENTS.md` 明确要求时，才继续展开读取 `docs/`、`SPEC`、`agent-plan` 等文档
