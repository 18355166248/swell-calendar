import { TimeGridData } from '@/types/grid.type';

export function GridSelectionByColumn({
  columnIndex,
  timeGridRows,
}: {
  columnIndex: number;
  timeGridRows: TimeGridData;
}) {
  return <div>GridSelectionByColumn</div>;
}
