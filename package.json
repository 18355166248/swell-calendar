{
  "name": "swell-calendar",
  "description": "日历组件",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check:docs": "node scripts/check-docs.mjs",
    "check:types": "pnpm --filter swell-calendar exec tsc --noEmit",
    "check:test:calendar": "pnpm --filter swell-calendar exec jest --runInBand",
    "check": "node scripts/check-docs.mjs && node scripts/check-arch.mjs && turbo run lint && pnpm --filter swell-calendar exec tsc --noEmit && pnpm --filter swell-calendar exec jest --runInBand",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=docs^... && changeset publish",
    "preview-storybook": "turbo run preview-storybook"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "eslint": "^9.29.0",
    "eslint-plugin-prettier": "^5.5.1",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "eslint-plugin-unused-imports": "^4.1.4",
    "prettier": "^3.2.5",
    "storybook": "^9.0.14",
    "turbo": "^2.5.4",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=18"
  }
}
