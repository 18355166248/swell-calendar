# swell-calendar-s2 · P7–P8 后续规划

> 本文件是 S2 外壳应用 P0–P6 全部完成后的下一阶段规划。
> 创建日期：2026-06-12

## 背景

P0–P6 已完成：脚手架搭建、设计稿忠实移植、S2 真组件替换、引擎挂活（scheduler / timeline）、
控件功能化（主题 / 搜索 / 筛选 / 新建 / 编辑删除）、数据源抽象。

当前存在两个主要体验断层：

1. **视图割裂**：scheduler / timeline 走 `packages/calendar` 真引擎（拖拽、resize、车道堆叠），
   但日视图 / 周视图 / 月视图仍是 `views.tsx` 里的静态 CSS 版——不可拖拽、不可滑动新建，
   与引擎事件列表也不共享同一份数据渲染路径。
2. **侧栏纯展示**："我的日历" 分类项和 MiniCalendar 均为静态 markup，无点击交互——
   无法按分类筛选可见事件，也无法通过迷你日历跳转日期。

`packages/calendar` 引擎**已经支持**日 / 周 / 月 / scheduler / timeline 五种视图，
且 scheduler 的滑动新建（`dragToCreate`）能力已内置，只是 S2 外壳尚未接通回调。
因此后续工作量集中在「接线」而非「造轮子」。

---

## P7 · 全视图引擎化 + 交互闭环

> 让日 / 周 / 月视图也走 `packages/calendar` 真引擎，并把 scheduler 滑动新建接通。
> 完成后所有五种视图共享同一份引擎渲染 + 同一份数据管线。

### 7a · 日视图 / 周视图 / 月视图接入引擎

**现状**：`App.tsx` 的 `useEngine` 只覆盖 `scheduler | timeline`；日 / 周 / 月走静态 `DayView` / `WeekView` / `MonthView`。

**目标**：把三种视图也交给 `<Calendar>` 渲染。

**改动范围**：

- `App.tsx`：
  - `useEngine` 扩展为全视图（移除 `useEngine` 条件，所有视图统一走 `<Calendar>`）。
  - `options` 按视图类型组装：day 走 `defaultView: 'day'`（**无独立 `options.day`**，日视图是「1 列的 week」，复用 `WeekOptions`——`hourStart` / `hourEnd` / `workweek` 均从 `options.week` 读取），week 走 `defaultView: 'week'` + `workweek`，month 走 `defaultView: 'month'`。
  - 移除 `<DayView>` / `<WeekView>` / `<MonthView>` 的条件渲染分支。
  - 所有视图的 `onEventClick` 统一走 `toPickEvent`（已有）。
- `calendarData.ts`：
  - day / week 视图的事件需要正确的 `Date` 对象（当前 `toCalendarEvents` 已经做了，无额外改动）。
- `views.tsx`：
  - 标记 `DayView` / `WeekView` / `MonthView` 为 deprecated 或直接移除（取决于是否还有 Storybook 使用方）。

**风险与注意**：

- **月视图交互受限**：引擎月视图目前不支持拖拽移动 / resize / 滑动新建（SPEC backlog），但点击事件和日期格渲染是完整的。作为 P7 可接受——先让月视图数据源和渲染与引擎统一，交互增强留后续迭代。
- **日视图的小时范围**：引擎 day 视图显示 0–24h 全天，设计稿 day 视图只显示 8–20h。日视图读取 `options.week.hourStart` / `options.week.hourEnd`（无独立 `day` 选项），现 App 已传 `week: { hourStart: 8, hourEnd: 20 }`，**日视图自动继承，零额外配置**。
- **SubBar 显示范围**：当前 SubBar（工作周开关 + 分类 chips）只在 week / day / scheduler 视图显示。引擎化后月视图也应显示分类 chips（用于 7b 的日历过滤联动）。

**验收**：

- 切换到日 / 周 / 月视图时，渲染来自 `<Calendar>` 而非静态组件。
- 事件数据、搜索过滤、分类过滤在五种视图中行为一致。
- `tsc --noEmit` / `vite build` / `pnpm check` 通过。

### 7b · Scheduler 滑动新建接通 ✅ 已完成

**现状**：引擎的 `dragToCreate` 能力已内置（`useGridSelection` → `createEventFromTimeGridSelection`），但 S2 外壳的 `callbacks` 只接了 `onEventClick`，`onEventCreate` 未接通——用户在 scheduler 空白处滑动后什么都不会发生。

**目标**：接通 `onEventCreate` 回调，让滑动选区结束后自动弹出新建对话框并预填时间 / 资源。

**改动范围**：

- `App.tsx`：
  - `callbacks` 新增 `onEventCreate`：从回调参数 `{ event }` 中提取 `start` / `end` / `resourceId`，转成 `EventDraft` 预填信息，然后打开 `CreateDialog`（复用现有新建对话框，增加 `initial` 预填值）。
  - `callbacks` 新增 `onEventUpdate`：把引擎的拖拽移动 / resize 结果转写回数据源（`updateEvent`）。
  - `callbacks` 新增 `onEventCreateFailed` / `onEventUpdateFailed`：可选，显示 toast 或忽略。
- `calendarData.ts`：
  - 新增 `engineEventToDraft(event: EventObject): EventDraft` 辅助函数，从引擎事件对象提取新建 / 更新所需的字段。
  - **更新场景必须基于原事件合并**：`toCalendarEvents` 已将原始 `CalEvent` 挂在 `event.raw` 上。`engineEventToDraft` 在更新路径下必须以 `{...(event.raw as CalEvent), day, start, end, res}` 的方式构建 draft——以原事件为底，只覆盖时间 / 资源。否则 `updateEvent` 的全量 override 语义会把 `title / cat / who / desc` 抹掉。新建路径无 `raw`，从引擎 event 字段直接构建即可。
- `overlays.tsx`：
  - `CreateDialog` 的 `initial` prop 已支持预填，无需额外改动（只需确认 `resourceId` → `res` 映射正确）。

**风险与注意**：

- **onEventUpdate 的日期格式**：引擎回调的 `event.start` / `event.end` 是 `Date` 对象，需要转回 `CalEvent.day`（dayIndex）+ 十进制小时。复用 `dateToDayIndex` / `timeToDecimalHour` 即可。
- **override 语义 + raw 合并**：`updateEvent(id, draft)` 内部是 override 全量替换（P5 语义）。**必须基于 `event.raw` 合并**（见上方 calendarData.ts 说明），否则拖拽只回写 time/resource，会丢失 title/cat/who/desc。
- **onRangeSelect 与 onEventCreate 会同时触发**：滑动选区在引擎内部**同时**触发 `onRangeSelect` 和 `onEventCreate`（`TimeGridView.tsx:222/232`）。外壳**只接 `onEventCreate` 即可**，不要两个都接导致双弹窗。
- **异步数据源延迟 × 拖拽回弹闪烁**：引擎是宿主受控、不维护乐观状态（PLAN4 §4.1），渲染只认 `props.events`。P6 的 `dataSource.ts` 每个写操作有 `180ms` 模拟 IO 延迟：drop → `onEventUpdate` → `updateEvent` → 180ms → 重拉 → 重渲。这中间事件会**先弹回原位、约 200ms 后才跳到新位**。解法二选一：① mutation 路径把 `IO_DELAY` 设为 0（只在首屏 `list()` 保留 loading 态）；② 在 `useCalendarData` 里做乐观更新（mutation 后立即 patch 本地事件列表，重拉后静默替换）。推荐方案 ①，工作量最小。

**验收**：

- 在 scheduler 空白区域按住鼠标拖出一段选区，松开后弹出新建对话框，时间和资源已预填。
- 拖拽事件到另一个资源列或不同时间段，事件位置更新并持久化。
- 拖拽事件底部边缘 resize 调整结束时间，更新持久化。

### 7c · Day / Week 视图引擎交互接通

**目标**：日视图和周视图也能滑动新建和拖拽移动事件。

**改动范围**：

- 与 7b 共用同一套回调（`onEventCreate` / `onEventUpdate`）。
- Day / Week 视图无 `resourceId`，新建时默认资源为空或使用第一个资源。
- 日视图无独立 options 键，小时范围复用 `options.week.hourStart` / `options.week.hourEnd`（已配置为 8–20，**零额外工作**）。

**验收**：

- 在日 / 周视图空白时段滑动新建，弹出对话框预填正确日期和时间段。
- 在日 / 周视图拖拽已有事件移动到其他时段，更新持久化。

---

## P8 · 侧栏交互完善

> 让左侧 "我的日历" 和 MiniCalendar 从纯展示变为可操作。

### 8a · "我的日历" 分类可见性切换

**现状**：`shell.tsx` 的 `navCal` 列表渲染四个分类（会议·评审 / 规划·设计 / 客户·对外 / 工程·协作），每个是一个 `<button>` 但没有 `onClick`——不可点击、不可切换。

**目标**：点击分类项可 toggle 该分类下事件的可见性（视觉反馈：关闭时变灰 / 划掉），与顶栏 SubBar 的分类 chips 联动。

**改动范围**：

- `App.tsx`：
  - 新增 `activeCalendars: Set<Cat>` 状态（初始全选），与 `activeCats`（SubBar chips）共用同一份集合或合并为单一状态。
  - 传入 `Sidebar` 的 props 增加 `activeCalendars` + `onToggleCalendar(cat)`。
- `shell.tsx`：
  - `SidebarProps` 新增 `activeCalendars: Set<Cat>` + `onToggleCalendar: (c: Cat) => void`。
  - `navCal` 按钮加 `onClick={() => onToggleCalendar(n.c)}`。
  - 视觉态：未选中时按钮加 `opacity: 0.4` + 色块变灰，文字加 `text-decoration: line-through`。
- 事件过滤：
  - `visibleEvents` 的过滤逻辑已经用 `activeCats` 做了分类过滤（P5 搜索过滤切片），只需确保侧栏 toggle 与 chips 操作的是同一份状态。

**风险与注意**：

- **状态一致性**：SubBar chips 和侧栏 "我的日历" 必须反映同一份 filter 状态。如果用户在侧栏关闭了 "会议·评审"，SubBar 对应 chip 也应同步取消选中（反之亦然）。建议合并为一个 `activeCats` 状态，两处 UI 都消费它。
- **侧栏与 SubBar 数量不对称**：侧栏 `navCal` 渲染 4 个分类（会议·评审 / 规划·设计 / 客户·对外 / 工程·协作），而 SubBar 的 `FILTER_CATS` 是 5 个（多一个「面试·招聘」）。合并时需确认 `navCal` 的分类键与 `FILTER_CATS` 对齐——要么侧栏补上第 5 项，要么接受「侧栏关不掉面试·招聘」的不对称。

**验收**：

- 点击侧栏 "会议·评审"，该项变灰，scheduler / 日 / 周 / 月视图中该分类的事件全部隐藏。
- 再次点击恢复显示。
- SubBar chips 与侧栏 toggle 完全同步。

### 8b · MiniCalendar 交互

**现状**：`shell.tsx` 的 `MiniCalendar` 是纯静态组件——月份标题硬编码为 "2025年3月"，左右箭头按钮无 onClick，日期格不可点击。

**目标**：

1. 月份切换：点击左右箭头翻月，标题跟随变化。
2. 日期选择：点击某个日期格，主视图跳转到该日期（`calRef.current.setDate()`）。
3. 今天标记：高亮当前选中日期 / 主视图正在展示的日期。

**改动范围**：

- `shell.tsx`：
  - `MiniCalendar` 改为受控组件：props 接收 `currentDate: Date` + `onDateChange: (d: Date) => void`。
  - 内部新增 `displayMonth` 状态（`useState(() => startOfMonth(currentDate))`），左右箭头改变 `displayMonth`。
  - 日期格点击回调 `onDateChange`。
  - 高亮逻辑：选中日期 + 所在周（沿用现有 `inWeek` / `today` 类名）。
- `App.tsx`：
  - 新增 `currentDate` 状态（初始 `2025-03-21`，与引擎 `initialDate` 一致）。
  - 当 `currentDate` 变化时，调用 `calRef.current.setDate(currentDate)` 同步引擎日期。
  - 当引擎 `onPageChange` 回调触发时（用户通过顶栏箭头翻页），反向同步 `currentDate`，让 MiniCalendar 跟着翻月。
  - **`canvas key={view}` 重挂载冲突**：现有 `<div className="canvas" key={view}>` 在切换视图时会整体重挂载 `<Calendar>`，引擎日期被重置回 `initialDate: '2025-03-21'`，刚导航的日期就丢了。解法二选一：① 把 `currentDate` 格式化后动态传入 `initialDate`（`initialDate={fmt(currentDate)}`），这样重挂载也落在当前日期；② 去掉 `key={view}`，改纯 `setView` 驱动（引擎内部 `ViewRouter` 已支持视图切换无需重挂载）。推荐 ②——避免无谓的重挂载开销，同时 ① 作为兜底保留。
- `Topbar`：
  - 顶栏 "今天" 按钮接通 `calRef.current.goToToday()` + 更新 `currentDate`。
  - 顶栏左右箭头接通 `calRef.current.navigate('prev'|'next')`。

**风险与注意**：

- **日期双向同步**：主视图翻日 ↔ MiniCalendar 翻月需要双向联动。引擎 `onPageChange` 回调可拿到当前日期，用于反向更新 MiniCalendar。MiniCalendar 点击日期时正向调用 `calRef.current.setDate()`。两个方向不能形成循环——`setDate` 触发 `onPageChange`，`onPageChange` 更新 state，state 更新不触发新的 `setDate`（通过 `useEffect` 依赖比较或 ref flag 防抖）。
- **月份计算**：需用 dayjs 或原生 `Date` 做月份加减，不引入新依赖（dayjs 已是 peer dep）。
- **固定单周 mock × 日期导航 = 跨周空白**：`data.ts` 的 `CalEvent.day` 是 0–6 的「周内索引」，`calendarData` 的 `makeDate` 把所有事件锚定在 `2025-03-17`（周一）那一周。后果：MiniCalendar 跳到 4 月 → 主视图能跳，但**没有任何事件**；月视图（2025-03）只有 17–23 那几格有事件。这不是 bug 但属于 UX 死角。**本切片显式 scope**：demo 仅 3/17 那周有数据，跨周 / 跨月导航为空属预期行为。如需跨周有数据，应把 mock 升级为绝对日期（回到 P6 真数据源的延伸工作），不在本切片范围。

**验收**：

- MiniCalendar 左右箭头可翻月，标题正确变化。
- 点击 MiniCalendar 某个日期，主视图跳转到该日（scheduler 显示该日、日视图显示该日、周视图跳到该周）。
- 主视图顶栏翻页后，MiniCalendar 高亮跟随。
- 点 "今天" 按钮回到当前日期。

---

## 优先级建议

| 阶段 | 切片 | 预估工作量 | 优先级 | 说明 |
|------|------|-----------|--------|------|
| P7 | 7a 全视图引擎化 | 中 | 高 | 消除最大的体验割裂（静态 vs 引擎） |
| P7 | 7b scheduler 滑动新建 | 小 | 高 | 引擎能力已就绪，只差回调接线 |
| P7 | 7c day/week 交互接通 | 小 | 中 | 复用 7b 回调，增量工作很少 |
| P8 | 8a 日历分类切换 | 小 | 中 | 与现有 chips 状态合并即可 |
| P8 | 8b MiniCalendar | 中 | 低 | 日期双向同步有一定调试量 |

建议执行顺序：**7b → 7a → 7c → 8a → 8b**。7b 最小最快（只加回调），验证引擎接线通畅后再做 7a 全视图切换。

---

## P7b 实施结果（2026-06-13）

**实际改动**：

- `App.tsx`：接入 `onEventCreate` / `onEventUpdate` / `onEventCreateFailed` / `onEventUpdateFailed` 四个引擎回调；新增 `createInitial` 状态用于预填新建对话框。
- `calendarData.ts`：新增 `engineEventToDraft()` 辅助函数，更新路径基于 `event.raw` 合并防止字段丢失，新建路径从引擎字段直接构建。
- `dataSource.ts`：将 mutation 路径（create/update/remove）的 IO 延迟从 180ms 降为 0，仅 `list()` 保留首屏 loading 延迟，消除拖拽回弹闪烁。
- `TimeGridView.tsx`：新增 `schedulerSelectionConstrainer`，scheduler 视图中跨列拖拽只允许同资源列选中，不同资源时自动吸附到当前日期的同资源列。
- `useGridSelection.tsx`：新增 `constrainPosition` 可选约束参数，在计算选区前对当前位置进行约束。
- `gridSelection.ts` / `grid.type.ts` / `gridSelection.type.ts`：`GridPosition` 和 `GridSelectionData` 新增 `allowedColumnIndices`，支持 scheduler 跨列选中时过滤中间异资源列。
- `scheduler.controller.ts`：`getSelectionColumns` 优先读取 `allowedColumnIndices`，跳过中间不同资源的列。
- `scheduler.controller.spec.ts`：补充跨天同资源拖拽和 `allowedColumnIndices` 资源名称汇总的单测。

**与原计划的偏差**：

- 原计划仅涉及回调接线（App + calendarData + dataSource），实际额外实现了 scheduler 视图的跨天同资源拖拽约束（`constrainPosition` + `allowedColumnIndices`），解决了跨不同资源列拖拽时事件错误归属资源的问题。

**验证结果**：

- `scheduler.controller.spec.ts` 新增 2 个测试用例通过。

**剩余问题**：

- `onEventCreateFailed` / `onEventUpdateFailed` 当前静默忽略，后续需补 toast 提示用户拒绝原因（overlap / invalid / policy）。

---

## P7a 实施结果（2026-06-13）

**实际改动**：

- `App.tsx`：
  - 移除 `useEngine` 门控（原 `view === 'scheduler' || 'timeline'`），五种视图统一走 `<Calendar>` 引擎渲染；删除 `<DayView>` / `<WeekView>` / `<MonthView>` 静态分支及其专用局部变量（`hourH` / `onPick`）。
  - SubBar 显示条件新增 `month`，月视图也展示分类 chips（为 P8a 日历过滤联动铺路）。
  - 移除 `DayView` / `WeekView` / `MonthView` 的 value 导入，仅保留 `type PickEvent`（仍用于 `pick` 状态与 `toPickEvent`）。
  - 顺带修正 scheduler 视图副标题，从「6项资源 · 会议室与成员」改为「3项资源 · 会议室」，与资源精简（6→3，仅保留会议室）对齐。
- `views.tsx`：保留不动。`DayView` / `WeekView` / `MonthView` 暂未删除（其同模块还导出 `PickEvent` / `evRange`，被 `calendarData.ts` / `overlays.tsx` 依赖）；静态视图组件现为未引用代码，后续清理另行处理。

**与原计划的偏差**：

- 计划提到「day 走 `defaultView: 'day'`、复用 `options.week` 小时范围」等 options 组装，实际这些在 P7b 已随 `defaultView: view` + `week: { hourStart: 8, hourEnd: 20 }` 一并就绪，本切片仅需移除 `useEngine` 门控即生效，无需新增 options 代码。
- 静态视图组件按计划「deprecated 或移除」中选择**保留**：因 `views.tsx` 仍承载被复用的 `PickEvent` / `evRange` 导出，直接删文件会牵连 `calendarData` / `overlays`。改为停止引用、留待后续独立清理切片。

**风险与注意（落地确认）**：

- 月视图引擎交互（拖拽 / resize / 滑动新建）仍受限，仅渲染与点击可用——符合计划预期。
- `canvas key={view}` 重挂载问题留待 P8b 处理，本切片不触碰。

**验证结果**：

- `tsc -p apps/swell-calendar-s2` 通过。
- `check-arch` 通过（178 文件无分层违规）。

---

## P7c 实施结果（2026-06-13）

**结论：零代码改动，纯验证切片。** P7c 的能力在 P7a + P7b 落地后已自动具备：

- P7a 让 day / week 走与 scheduler 同一份 `<Calendar>` + 同一套 `callbacks`，`onEventCreate` / `onEventUpdate` 对所有视图一视同仁（`TimeGridView` 是共用组件，scheduler 专属的只有 `constrainPosition` 资源约束）。
- `calendarData.ts` 的 `engineEventToDraft` 早已对「无 `resourceId`」路径兜底 `resources[0]?.id`（见 `calendarData.ts` 第 ~201 行），正好满足计划「day / week 新建默认用第一个资源」。

因此本切片只做浏览器实测确认，无需新增代码。

**验证结果（preview s2-app:5180 真实拖拽实测）**：

- **周视图滑动新建**：在周一空白时段（12:00–13:00）拖拽 → 弹出新建对话框，预填 日期=2025/03/17、时间=12:00–13:00、资源=会议室·海景厅（`resources[0]` 默认）；保存后事件正确渲染并持久化。
- **周视图拖拽移动**：把上述事件从 12:00 拖到 08:00 → 事件重定位到 08:00 并经数据源持久化（`onEventUpdate` → 基于 `raw` 合并写回，标题/分类不丢）。
- **日视图滑动新建**：在 3/21 空白时段（08:00–09:30）拖拽 → 对话框预填 日期=2025-03-21、时间=08:00–09:30、资源=会议室·海景厅，符合预期。

**注意**：

- day / week 新建对话框的资源默认指向第一个会议室（海景厅），属计划内可接受默认；day / week 本非资源维度视图，资源字段对其语义影响有限。
- 月视图仍不支持拖拽新建（引擎能力限制），点击事件正常——与 P7a 一致。

---

## P7c 跟进缺陷 · 跨天滑动新建（2026-06-13）

**现象**：周视图跨天滑动新建（如从周一拖到周二），确认后事件只落在起始天，结束天的跨度丢失。

**根因**：mock 数据模型 `CalEvent` 是单日结构（`day` + 十进制 `start`/`end`），结构上无法表达跨天；引擎产出的跨天事件（start 在 A 天、end 在 B 天）经 `engineEventToDraft` 被压成单一 `day`，`toCalendarEvents` 又把 end 渲染回同一天。叠加 `CreateDialog` 只有单个日期字段、且校验 `end <= start`（仅比时间）会直接把「次日更早的时间」误判为非法。

**修复**：

- `data.ts`：`CalEvent` 新增可选 `endDay`（省略时等于 `day`，向后兼容单日；跨天用 `endDay > day`）。
- `calendarData.ts`：
  - `toCalendarEvents` 的 end 改用 `makeDate(e.endDay ?? e.day, e.end)`。
  - `engineEventToDraft` 从引擎事件 end 的日期算出 `endDay`（新建 + 更新两条路径都显式写 `endDay`，更新路径防止 raw 旧值塌陷本次跨度）。
  - 把对话框 ↔ 草稿的转换函数（`inputToDraft` / `calEventToInput` / `engineEventToCreateInput`）从 `App.tsx` 迁入此处并补 `endDate ↔ endDay`，集中跨天字段、便于单测。
- `overlays.tsx`：`NewEventInput` 新增 `endDate`；`CreateDialog` 改为「开始（日期+时间）/ 结束（日期+时间）」两段式，校验改为比较完整时间戳 `\`${endDate}T${end}\` <= \`${date}T${start}\``，开始日晚于结束日时自动前移结束日。
- `App.tsx`：删除已迁出的本地转换函数，改从 `calendarData` 导入；精简相应 import。

**测试**（新增 `apps/swell-calendar-s2/src/calendarData.spec.ts`，10 用例全过）：

- `toCalendarEvents`：单日 / 跨天渲染（跨天结束落在 endDay 当天且晚于开始）。
- `engineEventToDraft`：跨天新建产出 `endDay > day` 且默认首个资源；单日 `endDay === day`；更新路径保留 raw 业务字段并刷新跨度。
- `inputToDraft` / `calEventToInput`：`endDate ↔ endDay` 双向守恒，单日默认对齐。
- 全链路回归：引擎跨天事件 → 预填输入 → 草稿 → 渲染，结束日始终保持在次日（锁住本缺陷）。

**测试工程接线**：s2 app 本机未装 vitest，`test` 脚本复用 `packages/calendar` 的 vitest 二进制（`node ../../packages/calendar/node_modules/vitest/vitest.mjs run`），并加入根 `check`（新增 `check:test:s2`）。spec 文件从 app `tsconfig` 的 `include` 排除（vitest 经 esbuild 自行转译，不依赖 app tsc）。

**验证**：`tsc -p apps/swell-calendar-s2` ✅、`vite build` ✅、`check-arch` ✅、s2 测试 10/10 ✅；浏览器实测周视图周一→周二跨天拖拽 → 对话框开始 3/17、结束 3/18，保存后渲染为「周一 12:00→当日底 + 周二 顶→11:30」两段。

### 跟进 · 跨天定时事件渲染到全天条（引擎修复，2026-06-13）

**现象**：跨天事件若时长 > 24h（如 18 号 08:00 → 20 号 10:00），保存后只在顶部「全天条」展示，不在时间网格下方按天铺开。

**根因（引擎层）**：`packages/calendar` 继承自 toast-ui 的旧规则——`isAllday` 把「`category:'time'` 且时长 > 24h」归为全天，`isTimeEvent` 又排除 `hasMultiDates`，于是 >24h 的定时事件被路由到全天栏、且被 day/week 时间网格的渲染过滤器剔除。（< 24h 的跨午夜事件不受影响，故之前 2 天跨度能正常分段。）

**修复（全局默认，经用户确认）**：

- `controller/event.controller.ts` `isAllday`：移除「time 时长 > 24h → 全天」分支，仅按显式 `allDay` / `isAllday` 判定。
- `model/eventModel.ts` `isTimeEvent`：移除 `!hasMultiDates`，跨天 time 事件仍算时间事件，交由时间网格按列裁剪分段（`setRenderInfo` 用 `maxTime/minTime` 把每段裁到当列当天可见范围）。
- `SPEC.md`：新增「多日 time 事件分段（day/week）」能力行与归类规则说明。

**影响面**：引擎全局——day/week/scheduler 的跨天定时事件统一在网格内分段；顶部全天栏只保留显式全天事件。`isTimeEvent` 仅 `setRenderInfoOfUIModels` 一处消费，`hasMultiDates`（宽度/排序/scheduler 分段）未改。

**测试**：

- `event.controller.spec.ts` +3：跨天 time 事件 `isAllday=false`、显式全天仍 `true`、`filterByCategory` 归到 `time` 面板。
- `eventModel.spec.ts` +3：`isTimeEvent` 对单日/跨天 time 均 `true`、allday `false`。
- 全量 285 个 calendar 测试通过，无回归。

**验证**：`tsc -p packages/calendar` ✅；浏览器实测 18 08:00 → 20 10:00，渲染为 3 段（18 起点→当日底、19 整列、20 顶→10:00），全天栏为空。

### 跟进 · 拖拽时间范围提示扩展到 day/week（引擎，2026-06-13）

**诉求**：拖拽（移动/缩放）时卡片/阴影要标明时间范围（如 `09:30 - 10:45`）。

**现状盘点**：移动阴影卡片本就用 `timeMove` 模板显示 `HH:mm - HH:mm`（全视图）；新建框选在选区内显示起止时间（全视图）；但浮层提示 `DragTimeTooltip` 被 `currentView !== 'scheduler'` 门控，且**缩放**阴影未传 `nextStartTime/Time` 给卡片——于是 day/week **缩放时完全没有时间范围提示**。

**修复**：`components/scheduler/DragTimeTooltip.tsx` 移除 scheduler 视图门控（该组件只在 TimeGrid 的移动/缩放阴影里挂载，无需按视图过滤）。现 day/week/scheduler 移动与缩放拖拽都跟随光标显示 `HH:mm - HH:mm` 浮层。`SPEC.md` 加「拖拽时间提示（time-grid）」能力行。

**测试**：新增 `DragTimeTooltip.spec.tsx`（3 用例）：week 视图拖拽显示范围、start/end 缺失不渲染、未拖拽不渲染。calendar 全量 294 通过。

**验证**：浏览器实测 week 视图缩放 产品双周评审 → 浮层显示 `09:00 - 12:00`。

### 跟进 · 跨天分段标签展示结束时间（引擎，2026-06-13）

**诉求**：跨天事件的第二天/第三天分段没展示结束时间，起始列也需要展示跨天的结束时间。

**根因**：`time` 模板只渲染 `开始HH:mm + 标题`。跨天事件各分段共用同一 model（真实起止），于是每段都只显示开始时间（如三段都 `08:00`）。

**修复**：`Template/default.tsx` 的 `time` 模板——当 `start`/`end` 不在同一日历日（`isSameDate` 判定）时，改为显示 `开始HH:mm - 结束HH:mm`，并附 `+Nd` 结束日偏移（`end.startOf('day') - start.startOf('day')` 的天数）。单日事件保持只显示开始时间。`schedulerTime` 委托 `time`，scheduler 同样受益。`SPEC.md` 对应能力行已补充。

**为何加 `+Nd`**：起始列分段视觉上从开始时间铺到当日午夜，若只写 `08:00 - 10:00` 会让 `10:00` 被误读为当天结束；`+2` 明示结束在 2 天后。

**测试**：`Template/default.spec.tsx` +3（单日只显开始、跨天显起止+`+2`、跨午夜相邻天 `+1`）。calendar 全量 297 通过。

**验证**：浏览器实测 18 08:00 → 20 10:00，三段标签均为 `08:00 - 10:00+2 三天测试`（起始列与第二/三天都显示结束时间）；单日事件仍只显示开始时间。

### 再跟进 · 跨天分段按列区分时间（参考 mobiscroll，2026-06-13）

**诉求修正**：不要每列都显示 `开始 - 结束`。跨天事件——起始列显示开始时间、结束列显示结束时间、中间列不显示时间；新建框选预览——第一列显示 `开始 - 结束`、最后一列显示结束、中间列留空。

**实现**：

- 保存事件（`TimeEvent` + `time` 模板）：`TimeEvent` 新增 `columnDate` prop（由 `Column` 透传），按「列日期 vs 事件起止日」算出 `segmentRole`（start/middle/end），注入 `time` 模板 param；`time` 模板据此显示开始/结束/仅标题。撤掉上一版「每段都 `HH:mm - HH:mm +Nd`」。单日事件无 `columnDate` 角色，行为不变。
- 新建框选预览（`GridSelectionByColumn`）：跨列时用 `timeGridSelection` 全局起止行取真实事件起止，起始列显示 `开始 - 结束`、结束列显示结束、中间列空字符串。

**测试**：`Template/default.spec.tsx` 重写为 4 用例（单日只显开始、start 列显开始、end 列显结束、middle 列仅标题）。calendar 全量 298 通过。

**验证**：浏览器实测——保存事件 18→20 三段为 `08:00 三天测试` / `三天测试` / `10:00 三天测试`；新建框选拖拽三列为 `08:00 - 10:00` / 空 / `10:00`。

### 再再跟进 · 跨天卡片两行布局 + 中间列「全天」（2026-06-13）

**诉求**：跨天卡片改两行——第一行标题、第二行时间；中间整天分段第二行显示「全天」。

**实现**：

- `Template/default.tsx` 的 `time` 模板：单日事件保持行内「时间 标题」；跨天分段（有 `segmentRole`）改为两行结构 `<span.event-time-multiday>`（子元素 `-title` / `-sub`）——start 列 sub=开始时间、end 列 sub=结束时间、middle 列 sub=`全天`。
- `css/events/time.scss`：新增 `.event-time-multiday`（`flex-direction: column`）+ `-title`（nowrap 省略号）+ `-sub`（11px、0.85 透明度）。**注意 CSS 改动需 `vite build` 重建 `packages/calendar/dist/style.css`**，s2 app 引用的是构建产物而非源 SCSS。

**测试**：`Template/default.spec.tsx` 扩到 8 用例（含 middle→`全天`、两行结构断言、单日不带 multiday 结构）。calendar 全量 300 通过。

**验证**：浏览器实测 18→20 三段 DOM——col18 `{title:三天测试, sub:08:00}`、col19 `{title:三天测试, sub:全天}`、col20 `{title:三天测试, sub:10:00}`，容器 `flex-direction:column`；单日事件仍行内单行。

### 再再再跟进 · 事件悬浮高亮 + 跨天多段联动（2026-06-13）

**诉求**：事件卡片悬浮加深；跨天事件悬浮任一段时，所有段一起加深表示鼠标经过。

**实现**：CSS `:hover` 只能命中鼠标直接经过的那一段，跨天多段是按列拆开的独立 DOM，需共享悬浮状态。

- 新增 `hover` slice（`types/hover.type.ts` + `slices/hover.slice.ts` + 接入 `store.type.ts` / `calendarStore.ts`）：`hoveredEventId` + `setHoveredEventId`。
- `TimeEvent`：`onPointerEnter/Leave` 调 `setHoveredEventId(model.id)/null`；用布尔 selector `hoveredEventId === model.id` 订阅，命中时加 `event-time--hovered` 类（仅状态翻转的段重渲染，高效）。
- `css/events/time.scss`：`&:hover, &--hovered` 同一组加深样式（投影 + `brightness(1.05)`）。**需 `vite build` 重建 dist/style.css**。

**测试**：`slices/hover.slice.spec.ts`（3 用例：初始 null、设置/清除、切换覆盖）。calendar 全量 303 通过。

**验证**：浏览器实测——保存 18→20 后悬浮第一段，三段 `event-time--hovered` 均为 `true`；移出后均 `false`（同一事件 id 联动）。

### 再×4 跟进 · 卡片透明度 + 配色收敛（2026-06-13）

**诉求**：卡片需要透明度（引擎层）；demo 配色过于花哨，收敛随机色让其更好看。

**实现**：

- 引擎透明度（`packages/calendar`）：`TimeEvent.getStyles` 移除静置内联 `opacity:1`（保留拖拽目标 `0.5`），改由 `events/time.scss` 控制——`.event-time { opacity: 0.9 }` 静置略透明、`:hover/--hovered { opacity: 1 }` 恢复不透明并加深。静置更轻、与网格/重叠融合，hover「加深」同时也是「实化」。**需 `vite build` 重建 dist/style.css**。
- 配色收敛（s2，`calendarData.ts` 的 `CAT_COLORS`）：把 6 个分类边框色 chroma 拉平到 ~0.09–0.10（原 magenta 0.19 / orange 0.16 最跳，已压下），填充统一为更浅更柔的 `L 0.955 / C ~0.028`，文字保持可读。保留 6 个语义分类的可区分度，去掉彩虹般的花哨观感。

**验证**：浏览器截图确认配色更克制协调（橙/品红不再突兀）；computed style——静置卡片 `opacity 0.9`、加 `--hovered` 类后 `opacity 1`。calendar 全量 303 通过。

### 再×5 跟进 · S2 demo 颜色源统一（2026-06-14）

**问题复盘**：

- 前一轮只改了 `calendarData.ts` 的引擎事件颜色，但 S2 外壳仍有第二套颜色源：
  - `data.ts` 的 `CAT_COLORS`（供 `shell.tsx` / `overlays.tsx` / `views.tsx` 消费）
  - `styles/spectrum-tokens.css` 的 `--cat-*` 变量（其值仍是旧的高饱和方案）
- 结果是同一个 `apps/swell-calendar-s2` 内，真引擎卡片和侧栏/弹层/静态残留视图可能出现颜色不一致；另外 `packages/calendar/src/css/events/time.scss` 实际静置透明度曾写成 `0.85`，与任务单 / SPEC 声明的 `0.9` 不一致。

**修复**：

- `apps/swell-calendar-s2/src/data.ts`：新增 `CAT_COLOR_STYLES` 作为 S2 分类颜色唯一代码源；原 `CAT_COLORS` 改为从该对象派生 line 色，兼容既有消费点。
- `apps/swell-calendar-s2/src/calendarData.ts`：停止内联硬编码 oklch，改复用 `CAT_COLOR_STYLES`，让引擎事件卡、资源色、`calendars` prop 与 S2 外壳完全同源。
- `apps/swell-calendar-s2/src/styles/spectrum-tokens.css`：把 `--cat-*` light/dark token 一并收敛到低饱和方案，确保 `shell.tsx` / `overlays.tsx` / `views.tsx` 与引擎卡片视觉一致。
- `packages/calendar/src/css/events/time.scss`：静置透明度修正为 `0.9`，与 `SPEC` / 任务文档 / 预期 computed style 对齐。

**结果**：

- S2 demo 内所有分类色消费点（引擎事件卡、资源头像、侧栏色块、popover 头像、分类 chips、静态残留视图）统一走一套收敛后的 token 方案。
- 引擎层 hover 仍为 `opacity: 1`，拖拽目标仍为 `0.5`，静置值与文档一致为 `0.9`。

### 再×6 跟进 · localhost 5173 的日/周/月卡片透明度统一（2026-06-14）

**诉求**：`http://localhost:5173/` 下日视图、周视图、月视图的卡片都要“有点透明度”，拖拽过程中的卡片也要保持半透明语义。

**落点确认**：

- 当前 `apps/swell-calendar-s2` 五个视图都已走 `<Calendar>` 真引擎，不再消费旧 `views.tsx` 的静态日/周/月卡片。
- 因此实际需要改的是引擎层：
  - day / week：`TimeEvent`
  - move / resize 拖拽引导：`MovingEventShadow` / `ResizingEventShadow`
  - month：`MonthEvent`

**实现**：

- `packages/calendar/src/css/events/time.scss`：保留静置 `opacity: 0.9`、hover `1`，新增 `event-time--guide` 为拖拽/resize 引导阴影提供半透明（`0.72`）。
- `packages/calendar/src/components/events/TimeEvent.tsx`：新增 `isGuideEvent`，用于给拖拽/resize 阴影卡打 `event-time--guide` 类。
- `packages/calendar/src/components/timeGrid/{MovingEventShadow,ResizingEventShadow}.tsx`：引导阴影统一传 `isGuideEvent={true}`，让 day / week 拖拽过程也保持轻透。
- `packages/calendar/src/components/month/MonthEvent.tsx` + `packages/calendar/src/css/month/monthGrid.scss`：month 卡片静置 `opacity: 0.9`，hover/focus 恢复 `1` 并加轻微阴影。

**结果**：

- localhost 5173 当前实际使用的 day / week / month 三种视图卡片现在都具备统一透明度语义。
- time-grid 拖拽时：原卡仍为 `0.5`，move / resize 引导阴影也不再是满实心卡片。

### 再×7 跟进 · time-grid 选区预览恢复半透明（2026-06-14）

**现象**：`http://localhost:5173/` 日视图里拖拽新建时，蓝色选区预览是整块实色，没有透明度。

**根因**：

- 引擎默认 `common.gridSelection.backgroundColor` 原本是带 alpha 的半透明色；
- 但 S2 app 在 `App.tsx` 里把它覆写成了纯 `var(--accent-tint)`，导致本地 demo 的选区预览失去透明度。

**修复**：

- `apps/swell-calendar-s2/src/App.tsx`：把 `common.gridSelection` 的覆写改成
  - `backgroundColor: color-mix(in oklch, var(--accent-tint) 58%, transparent)`
  - `border: 1px solid color-mix(in oklch, var(--accent-bg) 70%, transparent)`

**结果**：

- day / week / scheduler 的 time-grid 新建选区预览重新回到半透明表现，不再盖住底层网格内容。

### 再×8 跟进 · 顶部“日/周/月”切换器样式回归设计稿（2026-06-14）

**现象**：`http://localhost:5173/` 顶部 `SegmentedControl` 的“日 / 周 / 月 / 调度 / 时间线”切换器沿用了默认 S2 视觉，边框和选中态偏厚、偏重，和当前设计稿的轻量胶囊组不一致。

**修复**：

- `apps/swell-calendar-s2/src/styles/app.css`：为 `.s2-ss` 增加壳层样式覆写，改成浅底 + 细边框 + 999px 圆角的胶囊容器，统一 `3px` 内边距与 `2px` 间距。
- 同文件为 `.s2-ss button` 收口按钮高度、左右留白、字重、图标尺寸与 hover 色值；选中态改成白底卡片 + 轻阴影 + `accent-text` 前景，而不是默认更厚重的高对比方案。
- 保留 `:focus-visible` 的无障碍轮廓色，让键盘导航仍然清晰可见。

**验证**：

- 本地构建：`pnpm --filter swell-calendar-s2 build` 通过。
- 页面实测：`http://localhost:5173/` 顶部切换器 computed style 为：
  - 容器：浅底 `oklch(0.945 0.003 270)`、`border-radius: 999px`、`padding: 3px`、`gap: 2px`
  - 选中按钮：白底、`height: 28px`、左右 `12px` 内边距、轻阴影
- 验证截图已保存：`.codex-artifacts/topbar-segmented-after.png`

---

## 依赖与前置

- 所有 P7 工作依赖 `packages/calendar` 当前能力（已确认五种视图 + dragToCreate 均就绪），**无前置阻塞**。
- P8 不依赖 P7，理论上可并行推进；但 8b 的日期同步在引擎化视图下更自然，建议 P7 先完成。
- **8b 的 scope 受限于 mock 数据模型**：`CalEvent.day` 是周内索引（0–6），所有事件锚定在 2025-03-17 那周。MiniCalendar 可翻页可跳转，但跨周后视图为空。如需跨周有数据，需将 mock 升级为绝对日期或接入真后端——这是 P6 数据源抽象的后续延伸，不在本规划范围。
- 月视图的拖拽 / resize 交互增强在引擎 SPEC backlog 中，不在本规划范围内——等引擎侧交付后再补。
